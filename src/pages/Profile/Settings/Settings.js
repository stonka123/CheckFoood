import React from 'react'

const Settings = () => {
	let style = {
		backgroundColor: '#fff',
		color: '#2b2b2b',
		fontStyle: 'italic',
		textAlign: 'center',
		padding: '1em',
	}

	return (
		<div>
			<p style={style}>Ustawienia profilu aktualnie są wyłączone.</p>{' '}
		</div>
	)
}

export default Settings
